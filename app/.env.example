# Clerk — Frontend publishable key (safe to expose to the browser).
# Provisioned by the Founding Engineer on the Clerk dashboard (LINA-129 §7).
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxx

# API base for client-side calls (same-origin when running frontend+api together).
# When running the standalone Fastify API (apps/api), point to its origin.
NEXT_PUBLIC_API_BASE=http://localhost:3001

# App base URL used for links/callbacks.
APP_BASE_URL=http://localhost:3000
